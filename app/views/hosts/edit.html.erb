<% content_for(:title) { Copy::Admin::Hosts::EDIT_TITLE } %>
<%= render "shared/breadcrumbs", crumbs: [
  ["Wszyscy gospodarze", hosts_path],
  [@host.display_name, host_path(@host)],
  [Copy::Admin::Hosts::EDIT_TITLE, nil]
] %>

<h1 class="text-2xl font-semibold mb-4"><%= Copy::Admin::Hosts::EDIT_TITLE %>: <%= @host.display_name %></h1>

<%= render "form", host: @host %>
