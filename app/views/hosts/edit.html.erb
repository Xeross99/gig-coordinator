<% content_for(:title) { "Edytuj gospodarza" } %>
<%= render "shared/breadcrumbs", crumbs: [
  ["Wszyscy gospodarze", hosts_path],
  [@host.display_name, host_path(@host)],
  ["Edytuj gospodarza", nil]
] %>

<h1 class="text-2xl font-semibold mb-4">Edytuj gospodarza: <%= @host.display_name %></h1>

<%= render "form", host: @host %>
