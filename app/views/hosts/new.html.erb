<% content_for(:title) { t("admin.hosts.new_title") } %>
<%= render "shared/breadcrumbs", crumbs: [
  ["Wszyscy organizatorzy", hosts_path],
  [t("admin.hosts.new_title"), nil]
] %>

<h1 class="text-2xl font-semibold mb-4"><%= t("admin.hosts.new_title") %></h1>

<%= render "form", host: @host %>
