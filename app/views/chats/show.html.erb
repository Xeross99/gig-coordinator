<% content_for(:title) { "Czat — #{@event.name}" } %>
<%= render "chats/panel",
           event:    @event,
           messages: @messages,
           has_more: @has_more,
           message:  @message %>
