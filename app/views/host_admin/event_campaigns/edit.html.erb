<% content_for(:title) { "Edytuj serię" } %>
<%= render "shared/breadcrumbs", crumbs: [
  [ "Serie zleceń", host_event_campaigns_path ],
  [ @campaign.name, host_event_campaign_path(@campaign) ],
  [ "Edytuj serię", nil ]
], home_path: host_root_path %>

<h1 class="text-2xl font-semibold mb-4">Edytuj serię</h1>

<%= render "form", campaign: @campaign %>
