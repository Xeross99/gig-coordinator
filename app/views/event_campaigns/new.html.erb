<% content_for(:title) { "Nowa seria" } %>
<%= render "shared/breadcrumbs", crumbs: [
  [ "Nowa seria", nil ]
] %>

<h1 class="text-2xl font-semibold mb-4">Nowa seria</h1>

<%= render "form",
           campaign: @campaign,
           hosts: @hosts,
           url: event_campaigns_path,
           submit_label: "Nowa seria",
           users_for_prereg: @users_for_prereg %>
