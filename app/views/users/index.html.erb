<% content_for(:title) { "Wszyscy pracownicy" } %>
<%= render "shared/breadcrumbs", crumbs: [["Wszyscy pracownicy", nil]] %>

<h1 class="text-2xl font-semibold mb-4">Wszyscy pracownicy</h1>

<% if @users.any? %>
  <ul class="space-y-3">
    <% @users.each do |user| %>
      <% catches = @catch_counts[user.id] || 0 %>
      <li class="bg-white rounded-xl shadow-sm p-4 flex items-center gap-4">
        <% if user.photo.attached? %>
          <%= image_tag user.photo.variant(resize_to_fill: [56, 56]),
                        class: "size-14 rounded-full object-cover shrink-0" %>
        <% else %>
          <div class="size-14 rounded-full bg-stone-200 flex items-center justify-center text-stone-600 font-semibold shrink-0">
            <%= user.first_name[0] %><%= user.last_name[0] %>
          </div>
        <% end %>
        <div class="min-w-0 flex-1">
          <div class="font-medium text-stone-900 truncate"><%= user.display_name %></div>
          <div class="mt-1"><%= render "users/title_badge", user: user %></div>
        </div>
        <div class="flex items-center gap-1.5 shrink-0" title="Zaliczone łapania">
          <span class="text-lg leading-none" aria-hidden="true">🐔</span>
          <span class="text-sm font-semibold text-stone-900 tabular-nums"><%= catches %></span>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <div class="text-stone-500 py-8 text-center">Brak pracowników.</div>
<% end %>
