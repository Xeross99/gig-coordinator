<% content_for(:title) { Copy::Events::NEW_EVENT } %>
<%= render "shared/breadcrumbs", crumbs: [ [ Copy::Events::NEW_EVENT, nil ] ] %>

<h1 class="text-2xl font-semibold mb-4"><%= Copy::Events::NEW_EVENT %></h1>

<%= render "form",
           event: @event,
           hosts: @hosts,
           url: events_path,
           submit_label: Copy::Events::NEW_EVENT,
           users_for_prereg: @users_for_prereg %>
