<li class="bg-white rounded-xl shadow-sm overflow-hidden">
  <%= link_to event_path(event), class: "block p-4" do %>
    <div class="flex items-start justify-between gap-3">
      <div class="min-w-0">
        <div class="font-medium text-stone-900 truncate"><%= event.name %></div>
        <div class="text-sm text-stone-500 mt-0.5"><%= l(event.scheduled_at, format: :long) %></div>
        <div class="text-xs text-stone-500 mt-1"><%= t("events.host") %>: <%= event.host.display_name %></div>
      </div>
      <div class="text-right shrink-0">
        <div class="font-medium text-stone-900"><%= number_to_currency(event.pay_per_person) %></div>
        <div class="text-xs text-stone-500 mt-0.5"><%= event.capacity %> <%= t("events.seats") %></div>
      </div>
    </div>
  <% end %>
</li>
